# Локальный runtime

Сюда `scripts/setup_local_llm.ps1` устанавливает закреплённую сборку `llama.cpp`.
Приложение автоматически запускает `llama-server.exe` в скрытом режиме и обращается
только к `127.0.0.1`.
