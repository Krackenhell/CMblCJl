# Ускоренный локальный runtime

При наличии совместимого GPU `scripts/setup_local_llm.ps1` устанавливает сюда
официальную Vulkan-сборку llama.cpp. Если она не запускается, приложение автоматически
возвращается к CPU runtime из соседней папки.
