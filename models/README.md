# Локальная модель

В эту папку команда `scripts/setup_local_llm.ps1` загружает официальный GGUF-файл
`Qwen2.5-7B-Instruct Q4_K_M` (около 4,7 ГБ) и манифест с SHA-256.

Вес модели не хранится в Git из-за размера. После установки Meaning («Смысл») работает без
API-ключей и без доступа к интернету.
