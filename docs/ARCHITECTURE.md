# Архитектура VivaTrace

## Инвариант

Все семантические решения учебного цикла выполняет локальная LLM:

1. проверка исходного задания;
2. выбор ветки `viva` или `diagnostic`;
3. генерация вопросов;
4. оценивание каждого ответа;
5. создание задания на перенос или восстановление пробела;
6. предложение темы и плана следующей пары.

В runtime нет перехода на keyword-рубрику. Недоступность GGUF или llama.cpp останавливает проверку с явной ошибкой.

## Локальные модели

| Компонент | Значение |
|---|---|
| Быстрый интерактивный контур | Qwen2.5-3B-Instruct Q4_K_M, около 2,1 ГБ |
| Углублённый режим | Qwen2.5-7B-Instruct Q4_K_M, около 4,7 ГБ |
| Квантизация | Q4_K_M |
| Формат | GGUF |
| Runtime | llama.cpp |
| Интерфейс | локальный OpenAI-compatible HTTP на 127.0.0.1 |
| Интернет при inference | не нужен |
| API-ключ | не нужен |

`scripts/setup_local_llm.ps1` закрепляет версии и проверяет SHA-256. `src/vivatrace/local_llm.py` автоматически поднимает локальные серверы и принимает только структурированный JSON. По умолчанию весь синхронный путь использует 3B-модель; `LOCAL_LLM_QUALITY_MODE=1` переводит сложную проверку и финализацию на 7B.

## Состояния попытки

```text
DRAFT
  ↓ отправка ответа
LLM_SUBMISSION_ASSESSMENT
  ├── is_correct=true  → LLM_VIVA_QUESTIONS
  └── is_correct=false → LLM_DIAGNOSTIC_QUESTIONS
                          ↓
                 2 LLM-вопроса
                          ↓
                 LLM_EVIDENCE
                          ↓
                      BKT_UPDATE
                          ↓
               LLM_ADAPTIVE_ACTION
                  ├── TRANSFER
                  └── REMEDIATION
                          ↓
             LLM_COHORT_RECOMMENDATION
                          ↓
                        SAVED
```

## Разделение двух оценок

`Evidence.score` — качество конкретного ответа на конкретный вопрос сейчас.

`mastery[skill_id]` — накопленная вероятностная оценка BKT с учётом истории. Она может оставаться выше нуля после плохого ответа и не должна называться «точностью ответа».

Интерфейс показывает оба значения рядом, но с разными подписями. Выбор коррекционного задания опирается на полный LLM-анализ текущей попытки, а не только на старое mastery.

## Структурированные LLM-контракты

### Проверка задания

- `submission_score`;
- `is_correct`;
- `feedback`;
- `mode`;
- `skill_results`;
- `criterion_results` с точной ошибкой и исправлением;

Два отдельных коротких LLM-вызова получают оценку и создают персональные, самодостаточные вопросы: обоснование реально использованной формы и перенос правила. Разделение ролей даёт отдельные трассы оценщика и экзаменатора.

### Проверка ответа

- `verdict`, `score`, `confidence`;
- что верно и что нужно улучшить;
- корректный ответ и обработка опечатки.

Промпт отдельно фиксирует: совпадение отдельных слов, повтор вопроса, «не знаю» и случайный текст должны получать 0–0.10.

### Финализация

- `branch` (`transfer` или `remediation`);
- персональная активность студента;
- тема, обоснование и план занятия преподавателя.

Для remediation активность обязательно содержит отдельные `explanation`, `worked_example` и `practice_task`; промпт требует перепроверить их по рубрике и типичным ошибкам.

Чтобы малая модель не могла исказить базовое правило, remediation использует RAG-guardrail: LLM определяет пробел и `rule_id`, а объяснение, примеры и ссылка берутся из версионируемой карточки `english_b2_rules.json`. Ветка выводится только из LLM-оценок задания и viva, а не из ключевых слов ответа.

## Аудит

Для каждого вызова сохраняются:

- `trace_id` ответа llama.cpp;
- имя модели;
- SHA-256 GGUF;
- этап пайплайна;
- UTC-время;
- длительность;
- prompt/completion tokens, если runtime их вернул.

Аудит показывается в обоих кабинетах. Это техническое доказательство, но не гарантия педагогической корректности — её проверяют eval-набором и экспертной разметкой.

## Данные

SQLite содержит students, assignments, attempts и mastery. В attempts дополнительно хранятся оценка задания, ветка, персональное действие, групповая рекомендация и JSON-трассы.

Задания английского B2 лежат в `data/english_b2_assignments.json`, проверенные карточки правил и ссылки — в `data/english_b2_rules.json`, граф навыков — в `data/curriculum.json`.

## Риски

| Риск | Мера |
|---|---|
| Небольшая LLM ошибается | строгая рубрика, structured output, eval-набор, показ rationale, апелляция преподавателю |
| Модель засчитывает keyword matching | негативные тесты с бессмыслицей и перефразированием вопроса |
| Подмена LLM эвристикой | отсутствие fallback, trace ID и SHA-256 в каждой попытке |
| Медленная работа на CPU | 3B быстрый контур, постоянный сервер, короткие JSON-ответы; 7B включается отдельно на мощном ПК |
| Утечка данных | inference и SQLite полностью локальны |
| Смешение mastery и текущего score | разные поля, подписи и тесты |
